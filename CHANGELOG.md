# CHANGELOG

> 完整版本历史（与 GitHub Releases 同步；离线查阅用）。最新在上。
> 发版惯例：大改升主版本号；小修原地更新最新 release（含 zip 资产 `--clobber` 与 SHA256SUMS）。

## v4.0.0 · 新手三件套 + 设备识别 + 预适配矩阵 + 发布自动化

> Tag: `v4.0.0` · 2026-08-30

### 新工具（新手友好）

- **解锁向导** `tools/unlock_wizard.py`（tkinter GUI / 命令行双前端）：输 IP+管理页密码全自动——MiWiFi 登录算法取 stok（sha256 模式真机验证）→ start_binding 注入 → 验 22 口 → 一键接力部署；已解锁设备注入被拒（1541）有专门提示分支；subprocess 无 shell + 主机名校验
- **一键体检** `tools/kit_doctor.py` + `一键体检.bat`：零凭据局域网指纹扫描自动发现路由器 IP；固件/SSH/自愈/去广告//data 容量/云服务/弱密码/面板逐项体检；`--fix` 自动改写启动器 IP；主路由模式功能分支（WAN/UPnP/QoS/DHCP 绑定/端口转发）；结尾汇总"必须手动"待办
- **实机校准探针** `deploy/device_probe.py`：只读单连接输出设备画像，BE3600 到手后的校准入口

### 新架构

- `panel/device_profile.py` 设备识别解耦模块：机型能力表（`nvram get model`，RN07=AX3000E）+ 纯解析 + sh 注入；双面板接入、页脚显示套件版本号；未验证机型功能键不盲开

### 预适配矩阵

- `docs/BE3600-解锁与适配指南.md`：版本兼容矩阵（v1.0.87 需断电降级）、解锁方法 A/B/C、套件移植清单
- `docs/Mesh拓扑适配笔记.md`：无线/有线回程角色分工，去广告挂主节点、子节点分流自愈

### 工程与可信度

- GitHub Actions 双流水线：`ci.yml`（py_compile + sh -n + pytest）与 `release-latest.yml`（**小更新通道**：push 到 main 自动重建 zip+SHA256SUMS 覆盖上传最新 release）
- README 重写：适配设备表（≤1.0.24 版本策略）、"不是刷机项目"边界、双模式启动器平权、AI 辅助生成声明、CI/release/license 徽章
- `CHANGELOG.md` 全版本汇编；`SHA256SUMS.txt` 资产（zip 经 git archive 可逐字节复现）；Issue 模板（兼容性反馈/bug）；`panel/README.md` 目录说明
- 仓库更名 `xiaomi-router-unlock-kit`（旧地址自动重定向）；描述与 14 个 topics 补齐搜索缺口

### 依赖与修正

- paramiko 放宽 `<5,>=3`：4.0.0 真机连接/22 项测试/面板端到端全验证（5.0 才删 ssh-rsa）
- DNS 上游兜底池对齐实测结论（去 4.2.2.2，换百度 180.76.76.76）
- 实测结论入档：start_binding 对已解锁设备拒绝重复注入（1541），注入链仅锁定首解有效
- 卫生清理：LICENSE/SKILL 旧名、个人用户名路径、个人发版脚本移入 tools/

## v3.1.0 · DNS 日志开关 + NXDOMAIN 化 + 前端交互统一

> Tag: `v3.1.0` · 2026-08-28

### 路由侧

- **DNS 查询日志可开关**（AP 面板卡片按钮）：写/删持久化配置并重启 dnsmasq；执行结果自校验，SSH 失败如实报错不谎报成功
- **anti-AD NXDOMAIN 化（如实记录：无行为收益）**：刷新与部署链路把 `0.0.0.0`/空地址行转为 `/#`（10 万条已转换）。实测本机 dnsmasq 2.86 嵌入版对 `/#` **仍返回 0.0.0.0/::**——与主流文档的 NXDOMAIN 语义不同，属嵌入式构建差异；转换保留（与 AWAvenue 上游格式一致），但"降低客户端重试"的目标在此固件上未达成
- **上游 DNS 池九条收敛为四条国内精品**：223.5.5.5(阿里) / 119.29.29.29(腾讯) / 114.114.114.114 / 180.76.76.76(百度)。实测清除 4.2.2.2（被投毒：google 14ms 假应答 31.13.x）与 8.8.8.8/9.9.9.9/4.2.2.2（WU 域名解析到美国边缘，443 连接 196-1169ms vs 国内 9-29ms）
- **字节 DNS 180.184.1.1 清除，换入百度**：两轮实测多秒级停顿（tlu.dl 3.4-4.2s×3）；字节自家 DNS 对自家域名无优势（抖音走 HTTPDNS，公共解析器拿到的 CDN 边缘一样好）
- **抖音定向字节 DNS 复活载体全部删除**：auto_ssh.sh 恢复行、一键部署段落、bytedance.conf 模板一并清除（永不复活）
- `auto_ssh.sh` 升 **v6**（含 `/#` 转换逻辑），存量列表一次性转换

### 前端

- 主面板交互统一：**fetch + toast + 局部刷新**，删除 form/302/`?msg=` 整页刷新链路
- 主面板 GET / 服务端零 SSH：渲染从 187-300s 降至 **1.8ms**
- AP 面板：配置加载失败显示错误状态 + 点击重试；`doAct` 必填校验修复（缺字段不再静默提交）
- 修复浏览器按钮自 v2.5 起静默失败的回归（`/api/act` 表单 + JSON 双解析）
- **恢复共享层 HTML 热加载**（v2.5 特性，v3.0 重构时回归）：模板每请求读盘，改 .html 无需重启面板

### 测试与文档

- 新增 `/api/act` 应答格式 3 项端到端测试（真实 HTTP 回环），合计 **17 项**，CI = 语法 + pytest
- 维护 SKILL 全面更新至 v3.1.0 后状态：架构（共享服务层）/铁律（连接限流+写后自校验+单实例加载器）/DNS 结论/发版三步同步流程

### 已知边界

- 主面板查 AP 模式路由器取数慢（模式不匹配，属预期）；主路由模式下单次采集 ~284ms
- ipset 阻断在中继模式不适用（阻断点须在网关，即上级路由）——已探测固件支持但架构上用不上，详见 README「定位与边界」

## v3.0.1 · /api/act 应答格式修复（复审三处硬伤）

> Tag: `v3.0.1` · 2026-08-28

复审 v3.0.0 发现共享层两个实现 bug 与一个测试隐患，本版修复：

### 修复

1. **解析与应答判定不同源**：`parse_act_body()` 按 body 首字符判 JSON，应答却看 `Content-Type`——`curl -d '{...}'`（默认 form 头）会拿到 302 空响应。现改为 `parse_act_body` 返回 `(action, params, is_json)`，**应答与解析用同一判定**。
2. **表单报错裸渲染 JSON**：`do_action` 抛异常时无论请求形态都返回 200 JSON，浏览器按钮出错直接显示原始文本。现按 `is_json` 分流——表单请求 302 回跳 `/?msg=操作出错: ...`，与成功路径一致。
3. **测试加载器双实例隐患**：面板 `import monitor_web` 与测试的 importlib 加载会产生两份模块对象。现测试先把真实模块注册进 `sys.modules["monitor_web"]`，面板复用同一份。

### 新增测试（17/17）

- 案例①：JSON body + form Content-Type → 200 JSON（真实 HTTP 回环）
- 案例②：表单请求 + `do_action` 抛异常 → 302 带友好消息
- 表单成功 → 302；以及既有 14 项全保持

### 真机验证（2026-08-28）

- AP 面板重启至本版：curl 无 `Content-Type` 的 JSON POST → **200 `{"ok":true,"msg":"dnsmasq 已重启"}`**
- 同源表单 POST → **302 → /?msg=**，浏览器内操作出错也会回面板展示友好提示

## v3.0.0 · 架构统一 + 测试体系

> Tag: `v3.0.0` · 2026-08-28

### 架构（双面板单一来源）

- `monitor_web.py` 成为两个面板唯一的 HTTP 服务层：HTTP Basic 认证、Host 回环校验、Origin/Referer 同源校验、路由与应答格式**全部单一来源**（v2.5 起两边各改各的安全层，主面板就因此漏过一轮）
- 主面板删除 185 行重复 Handler/认证实现，改为 ctx 注入路由（page 动态渲染 / api / config / act）
- `/api/act` 同时接受 **JSON 与表单**提交，按 Content-Type 分流应答（JSON→200、表单→302 经 `?msg=` 回跳）——修复 v2.5 重构后**浏览器按钮静默失败**的回归

### 测试

- 新增 `tests/test_security.py`：**14 项本地单元测试**（不连路由器）——注入参数拒绝×6、恢复白名单（穿越/绝对路径/精确匹配）、内联 JSON 转义、Host/Origin/Basic 门禁、请求体双解析
- CI（sanity）从"仅语法"升级为 **语法 + pytest**

### 验证（2026-08-28 23:0x 真机）

- AP 面板（共享层上线后重启）：T1 全清单通过——备份/恢复 89 项、双列表更新无 `.new` 残留、LED 保存 crontab 行数不变、custom.conf 双落点
- 主面板 `--lan --token` 冒烟：401 / 200 / 403 / 302 + 表单 302 回跳全过
- 浏览器表单路径端到端：`302 → /?msg=dnsmasq 已重启`（自 v2.5 起静默失败的按钮回包已复活）
- 单元测试 14/14 通过；期间自审抓到并修复 `api_snapshot` 缺失迁移（旧内联实现未带过来会导致启动即崩）

### 边界（如实说明）

- 主面板查 AP 模式路由器取数慢（模式不匹配，属预期）；主路由模式下单次采集 ~284ms
- WiFi 信道/功率为即时切换，重启丢失（官方框架覆盖，持久请用小米管理页）
- 去广告为 dnsmasq `address=` 方案，约 5-15MB 内存（256MB 机型可承受；ipset 集合为后续可选项）

## v2.6.0 · 主面板认证重做(HTTP Basic) + XSS 修复 + 开箱即用包

> Tag: `v2.6.0` · 2026-08-28

## 修复

- **`--lan` 认证回归**（v2.5.1 引入）：自定义令牌头前端不会携带，局域网模式下页面能开、API 全 403。改为与 AP 面板同款 **HTTP Basic**——401 + `WWW-Authenticate`，浏览器原生弹框，凭证自动附带所有请求；`hmac.compare_digest` 常时比较。`--lan --token` 约束不变，localhost 免令牌不变。
- **内联 JSON 存储型 XSS**：配置 JSON 直插 `<script>window.__CFG__=`，`</script>` 可被 DHCP 主机名截断注入（主机名是客户端自报的，未校验）。内联前 `<` / `>` / U+2028 / U+2029 全部逃逸。

## 验证

- 编译通过；转义单元测试：含 `</script>` 的主机名内联后无裸尖括号
- `--lan --token` 冒烟：无凭证 401 / 错密码 401 / 正确凭证 200 / 恶意 Origin 403 / CLI 直连 302
- GET 配置页 200（187s——主面板查 **AP 模式**路由器属模式不匹配，属预期；主路由模式下单次采集 ~284ms）

## 开箱即用

- **`ax3000e-unlock-kit-v2.6.0.zip`**：解压 → 双击 `panel/Start-AP-panel.bat`（中继/AP，日常推荐）或 `Start-MainRouter-panel.bat`（主路由）→ 首次自动装依赖，输 IP 和 SSH 密码即用
- **`auto_ssh.sh`**：单件下载，放 `/data/auto_ssh/` 后 `install`

## v2.5.1 · 主路由面板安全加固（对齐 AP 基线）

> Tag: `v2.5.1` · 2026-08-28

## 安全加固：主路由面板对齐 AP 面板基线（09c197b）

外部审计发现主路由面板 `router_monitor_ax3000e.py` 缺失 AP 面板的全套安全机制，在主路由模式下构成**局域网未授权 RCE**（绑定全网卡+零认证+五处 uci 单引号拼接注入+存储型 XSS）。本次补齐：

### 修复项
- **默认绑定 127.0.0.1**（原为 `::` 双栈全网卡）；局域网访问需显式 `--lan --token <令牌>`，缺令牌拒启
- **Host 回环校验**（防 DNS rebinding）+ **Origin/Referer 同源校验**（防跨站 CSRF）接入所有 GET/POST；--lan 模式强制令牌（POST 头 `X-Panel-Token` / GET `?token=`）
- **五处 uci 注入全堵**：port_add / device_bind / fw_rule_add / dhcp_lease / qos_band 全部参数白名单化——IPv4+每段≤255、MAC 正则、端口 1-65535（支持范围段）、协议枚举、名称限 32 位安全字符、租期 `数字+h`、带宽整数 0-1000000
- **wifi_channel 信道分段校验**：2.4G 限 1-13，5G 限 32-177（原 1-177 一刀切）
- **存储型 XSS 修复**：前端 renderCfgBody 全部路由器返回字段（设备主机名/MAC/绑定名/端口转发/自定义域/cron 行/SSID）经 E() HTML 转义后渲染
- **custom.conf 持久化同步**：主面板自定义屏蔽增删现已同步 /data/custom.conf（对齐 AP 面板 497b399）

### 验证记录（2026-08-28）
- `py_compile` 通过；本机 8789 冒烟：GET 200、**经局域网 IP 访问被拒**（仅回环）
- 注入用例 6/6 被参数校验拦截（`1.1.1.1'$(reboot)'` 类 payload 均未触及 uci）
- 恶意 Origin POST → 403；同源 POST → 302 正常回跳

### 建议
日常使用仍推荐 **AP 面板**（迭代最活跃）；主路由面板现可安全用于主路由模式场景。

## v2.5 · 备份恢复入口 + 定时任务列表修正

> Tag: `v2.5.0` · 2026-08-28

## 新功能：备份恢复入口（补上最后一块）

- 备份列表每条新增"恢复"按钮：从本地备份把路由器配置恢复回去
- 恢复链路五道防线：
  1. **白名单精解**：只恢复 /etc/config、dnsmasq.d、crontab、自愈脚本、DNS 列表缓存等配置路径，本地重打瘦身包，备份里的 yhosts 遗迹（99-adblock.conf / adblock.hosts）不会被复活
  2. **恢复前自动备份现状**（失败即中止，绝不在无保险时动手）
  3. **/data 净增空间预检**：只算新增字节（覆盖已有文件不算增长），放不下直接中止——针对 1.7MB 小卷的防撑爆设计
  4. **分块上传 + 字节数回读校验**：校验不过不动路由器任何配置
  5. 解压后自动清理遗迹、修正自愈脚本权限、重启 cron 与 dnsmasq

## 修正

- 定时任务列表中的 LED 定时行改为只读展示（"由 LED 面板管理"）——此前显示的删除按钮点了无效（cron_del 只认 #panel 标记），现已消除这个误导性按钮；LED 定时统一在 LED 面板增删改

## 补丁（审查后加固）

- **白名单补漏**：data/microsoft.conf（DNS 优选配置）原缺失——备份里有、恢复时被排除，重启后微软系域名专用 DNS 路由会静默丢失。已补入并实测：备份包含该文件，恢复后完好
- **白名单收紧**：文件项精确匹配（etc/crontabs/rootxxx 不再误中）、目录项按 / 边界前缀匹配、拒绝 .. 路径穿越与绝对路径成员（10 用例全过）
- **失败分支修正**：解压失败时不再重启任何服务（原逻辑会造成半恢复+服务已重启的混合态），提示明确可重试
- **提示语补全**：恢复确认与结果文案注明非时点回滚（只覆盖不删多余文件）、/etc/config 内网络与 WiFi 改动需重启路由器生效

## 补记：自定义屏蔽域名持久化（commit 497b399）

- 添加/解除屏蔽即时写 /tmp/dnsmasq.d/97-custom.conf 并同步 /data/custom.conf，路由器重启后由自愈脚本的恢复块自动回挂
- 本地备份/恢复白名单已覆盖 custom.conf（bd4779f），恢复不丢
- 实测（2026-08-28 21:3x）：添加 test.example.com → 两落点各命中 1 行 → 解除屏蔽 → 两落点清零；dnsmasq 重启正常

## 实测记录（幂等回环）

- 备份 → 立即恢复同一备份，全链路通过：90 项配置恢复（加固后 88 项，口径一致）
- crontab / 自愈脚本 / dnsmasq.d 清单恢复前后 md5 完全一致
- /data 占用恢复前后一致，dnsmasq 与 crond 存活
- yhosts 遗迹与上传残留均为 0；microsoft.conf 恢复后完好
- 2026-08-28 21:31 复测：restore router_backup_20260828-212937.tar.gz → 89 项恢复，恢复前自动备份触发（router_backup_20260828-213130.tar.gz），crontab 15 非空行不变、自愈脚本 md5 前后一致、双列表（100186+883）完好在跑

## v2.4 · 去广告换源 AWAvenue + /data 卷满防护 + 维护 SKILL 入仓

> Tag: `v2.4.0` · 2026-08-28

## 去广告列表换源（yhosts → AWAvenue）

- yhosts 上游（VeleSila/yhosts）2025-03 已归档停更，本机快照停留在 2022-04，且 90% 域名与 anti-AD 重合，此前也没有任何更新通道
- 新增 `90-awavenue.conf` 槽位：开机按 48h 年龄自动刷新 + 每日 04:30 定时刷新，jsDelivr/raw 双镜像回退，体积/条数双门槛校验（下载异常不覆盖在用缓存）
- 抖音豁免域过滤后 883 条已上线，实测劫持生效；yhosts 相关文件（99-adblock.conf / adblock.hosts）已退役删除
- 路由器直连镜像失败时可 PC 代拉：curl 多镜像回退 → gzip → base64 分发（本次实测路径）

## 关键防护：/data 小卷写满导致的静默清空（重大事故根因修复）

- 根因确认：`/etc/crontabs` 与 `/data` 同在一个仅 1.7MB 的 UBIFS 卷，卷满时 `cat > 文件` 截断成功、写入静默丢失、命令照常返回 0 —— 8/28 两次 crontab 被清空均源于此
- `auto_ssh.sh` 落盘方式改为 `/tmp 暂存 → 删旧 → cat 写新`，杜绝 `.new` 双份占用峰值
- 面板 `sh_write()` 增加写后回读字节数校验，校验不过返回失败（LED 定时/任务删除等所有落盘路径生效）
- SKILL 铁律新增：写路由器文件前先 `df /data`

## 部署与工具修正

- `一键部署.bat` 原指向已改名的脚本（双击必失败）→ 修正；占位符密码字面量 → 改环境变量或交互输入
- 维护 SKILL 入仓 `skill/ax3000e-router-maintainer/SKILL.md`（MAC 已脱敏），任何 agent 可即插即用接手维护

## 现状快照（2026-08-28 实测）

- anti-AD 100,186 条 + AWAvenue 883 条双列表在线，baidu 正常解析
- crontab 16 行完整（auto_ssh 每分钟兜底 + 04:30 刷新）
- /data 占用 80%（修补后不再逼近 100%）


## 补丁（v2.4.0 就地追加）

- **面板落盘与 auto_ssh v5 对齐**：update_antiad / update_awavenue 改为 /tmp 暂存→删旧→cat 写新，消除 .new 双份占用（1.7MB 卷的撑爆隐患）；下载失败不再残留半成品；去广告关闭时同步清理 /tmp/*_new.conf
- **LED 定时误删防护**：只删面板管理的标准格式行（严格正则），用户手写的其它 led_ctl 定时不再被误伤；定时行带 #panel-led 标记并显示在定时任务面板；crontab 读取为空时拒绝写入
- **实测记录**：anti-AD 100,186 条 + AWAvenue 883 条双列表实机更新成功，/data 与 /tmp 零残留；LED 定时往返测试 15 行写前=写后、非 LED 行零丢失；sh_write 写入校验正反例均通过

## v2.3 · 安全加固（CSRF/Host）+ 去广告真开关 + 备份补全 + 部署修正

> Tag: `v2.3.0` · 2026-08-28

本版针对一轮逐条实测核对过的缺陷审查。审查 14 条中 12 条成立，2 条系审查方看了旧提交（bytedance 空格、面板文件名不存在）——均已实码核验。

## 安全（根因级）

- **CSRF 防护**：`/api/act`、`/api/nettest` 等 POST 强制校验 Origin/Referer 与 Host 一致。此前恶意网页可用 `text/plain` 简单请求（不触发 CORS 预检）跨站打 `127.0.0.1:8787`，配合 `cron_add` 可让路由器一分钟内以 root 执行任意命令（RCE）；绑定 localhost 挡不住这条链，现已堵死
- **防 DNS rebinding**：本地模式校验 Host 头必须是回环地址；无 Origin 的 POST 仅在 localhost 模式按 CLI 放行
- 攻击面实测：恶意源 POST→403、rebinding Host→403、同源→放行

## 功能修正

- **去广告开关名实相符**：现在同时移除/恢复 anti-AD 主列表（96，10 万条）与 yhosts（99）；新增 `/data/.adblock_off` 持久标志，`auto_ssh.sh` 开机尊重关闭状态（关闭后重启也不会被拉起）；面板状态读取改按标志文件。实测 off→on 往返：标志增删正确、96/99 按预期移除/恢复、恢复后广告域名实测劫持 0.0.0.0
- **备份补全**：tar 纳入完整 `/data`（此前漏 antiad.gz/bytedance/noipv6/logqueries/microsoft），实测新备份 241 项全命中；恢复入口列入后续计划（见 Known）
- **面板连接状态可见**：采集连败 3 次后芯片显示"路由器SSH断"（红点），不再静默全 0

## 可靠性

- `sh()/sh_write()`：命令已下发后读流超时**不再自动重发**，杜绝非幂等命令（追加配置/重启服务）被执行两次；仅连接未建立时重试
- CPU 统计补全 `softirq/steal` 字段（`p[1:9]`）
- 部署脚本：按检测到的模式选择 ap/ax3000e 面板启动，凭据经环境变量传入（不再要求手改文件）；anti-AD 首装加入与面板同标准的体积(>500KB)/行数(>1000)门槛，下载异常时不覆盖好缓存；auto_ssh 上传前统一转 LF + 新增 `.gitattributes`，根治 Windows 检出 CRLF 把路由器 shell 脚本搞挂的问题

## 校验补严

- `dns_latency` 校验应答 transaction ID
- `dns_add` 校验 IP 各段 ≤255（拒绝 999.999.999.999）
- WiFi 信道分频段限域：2.4G 1-13、5G 32-177
- 网络测试标签如实化："本机ping外网 X ms"、"路由器侧解析(含SSH往返) X ms"

## Known（未在本版）

- 面板"从备份恢复"入口（当前恢复按 docs 自救手册手工做）
- `--lan` 模式 HTTP Basic 明文（局域网嗅探风险，已在 README 注明；要彻底解决需 HTTPS/一次性令牌流）

## 升级方式

`git pull` 后重启面板即可；路由器上的 `auto_ssh.sh` 若用 v2.3 一键部署重跑会自动更新（本次发布机已实弹同步并 `sh -n` 校验通过）。

## v2.2 · 面板健壮性修复：注入校验 / 去广告缓存保护 / 配置中心单次往返

> Tag: `v2.2.0` · 2026-08-28

本次为面板健壮性修复版：把代码审查中发现的 9 个问题全部处理，其中 2 个是会真正导致故障的 bug。所有改动均已在实机（AX3000E / 1.0.24 / 中继模式）上验证。

## 高危修复

### 1. antiad_update 下载失败会清空去广告缓存
- 原逻辑：`curl` 下载后不校验结果，直接 `grep` 过滤写入生效路径，再 `gzip -c > /data/antiad.gz`。网络不佳时下载得到空文件，好的持久缓存被空列表覆盖，重启自愈后恢复的是一份空广告列表，而且界面不报错、用户无从察觉。
- 现在：先下载到 `/tmp/antiad_raw`，用与 `auto_ssh.sh` 相同的 **500000 字节门槛** 判定下载完整性；过滤后再校验 **行数 ≥ 1000**；两步任一不达标就删除临时文件并直接返回「已保留原有 anti-AD 缓存」，绝不触碰生效配置。
- 覆盖动作改为原子替换：`96-antiad.conf` 先 `mv` 到位，持久缓存写 `antiad.gz.new` 成功再 `mv -f` 换名，中途失败不会留下半截文件。
- 顺带修掉根因：`curl --max-time 90` 远大于 SSH 执行的默认 10 秒读超时，原实现会在读流超时后判失败并**重复触发一次下载**（这正是空文件出现的路径）。已给该步单独放宽到 100 秒。
- 实测：一次完整更新 13.9 秒返回 `anti-AD 已更新: 100186 条`，dnsmasq 正常在跑。

### 2. wifi_channel 存在命令注入
- 原逻辑只判断参数是否等于 `"0"`，未校验是否为数字。传 `6; reboot` 会拼成 `iwconfig wl0 channel 6; reboot`。
- 现在补 `isdigit()` 与 **1-177 合法信道范围** 双重校验，非法输入直接返回提示、不落到命令行。
- 实测：`6; reboot` → 「信道须为数字」；`999` → 「信道范围 1-177」；`0` → 原有自动模式提示。

## 性能

### 3. get_config() 由 20+ 次 SSH 往返合并为 1 次
- 采集侧早就做了单次往返，但配置中心每次打开仍要发 20 多条独立命令（`cat` / `uci` / `ps` / `wc` / `ip neigh` / `ip6tables` ……），和 3 秒采集循环抢同一把 SSH 锁，表现为点开配置页明显卡顿。
- 现在全部只读状态用 `@@` 分隔符拼成一条命令一次取回、本地解析；DNS 查询日志的「超限截断 + SIGHUP + 取尾部」也并入同一次往返。
- 实测：`/api/config` 端到端 **约 310 ms**，返回字段与结构完全不变（上游 8 个、anti-AD 100186 条、yhosts 6428 条、在线设备 2 台、查询记录 40 条、LED 定时 08:00/00:00 等均正常）。

## 一致性与安全写法

### 4. ad_custom_del 误删风险与临时文件竞态
- `grep -v 'address=/example.com/0.0.0.0'` 里 `.` 是正则通配符，理论上会连带删掉 `exampleXcom` 这类相似行；且固定使用 `/tmp/c.tmp` 存在并发覆盖风险。
- 改为与 `cron_del` 一致的「读取 → 本地精确整行比对过滤 → 经 stdin 写回」，不再依赖远端正则。
- 实测：新增 `panel-selftest.invalid` 后列表出现该域名，删除后恢复为空，文件其余内容未被误动。

### 5. dns_del 统一为安全写回
- 原 `sed -i '/server=...$/d'` 功能上没错（`re.escape` 产出的 `\.` 在 sed BRE 中合法），但与 `cron_del` 写法不一致，维护时容易困惑。同样改为本地过滤 + 写回。
- 额外加一条保护：拒绝删空全部上游（否则域名解析会直接中断，需重启才恢复）。
- 实测：加 `8.8.8.8` 再删，上游列表精确回到原 8 个。

### 6. run_net_test 的 ping 参数不跨平台
- `-n`（次数）/ `-w`（毫秒超时）是 Windows ping 专有，Linux/macOS 应为 `-c` / `-W`（秒），原代码在非 Windows 上延迟测试必然失败。
- 现按 `os.name` 选择参数，并同时解析 Windows 的「平均 = N」与 POSIX 的 `min/avg/max` 两种输出。
- 实测（Windows）：`延迟 223.5.5.5: 11 ms`、`延迟 119.29.29.29: 14 ms`、`DNS解析: 20 ms`。

### 7. cache_set 非法输入返回 500
- `str(int(params.get("size", 1024)))` 传非数字抛 ValueError，被外层吞成一条 500 错误。
- 改为按字符串校验并限定 **64-100000**，非法时返回可读提示。实测 `abc` 与 `10` 均正确拒绝。

### 8. get_dns_stats 的固定 sleep(1.5) 不可靠
- 发 `SIGUSR1` 后死等 1.5 秒再读，路由器负载高时转储尚未落盘，读到的是上一次的旧统计。
- 现把等待循环下放到路由器侧：先记录既有转储行数，发信号后每 0.2 秒轮询一次、出现新行立即返回（最多 5 秒），日志文件不存在时马上跳出避免空等。仍保持单次 SSH 往返。
- 实测：**248 ms** 返回 `rate 49.3%`（本地应答 2514 / 转发 2588 / 缓存 4096）。

### 9. 设备链路探测选机逻辑不可靠
- 原实现遍历 `dhcp.leases` 取「最后一个」非网关 IP，注释称是最近接入的设备，但租约文件并不保证按时序排序，且字段顺序随固件版本变化。
- 改为直接读 ARP 邻居表，按 `REACHABLE > STALE > DELAYED > PROBE` 优先级挑一台确实可达的主机。
- 实测：正确选中 192.168.2.102 并完成探测。

## 同步范围

主路由模式面板 `router_monitor_ax3000e.py` 存在同一批 handler，已同步 #1、#2、#4、#5、#7 五处修复（该文件保留 hagezi 列表源，下载改为先落临时文件、校验 ≥1000 条再原子覆盖）。配置中心往返合并等性能改造仅应用中继面板，主路由面板未在实机验证故未改动。

## 升级方式

拉取本 tag 后替换 `panel/` 下两个文件即可，路由器侧无需任何改动；面板仍依赖 `paramiko<4`。

## v2.1 · 面板降载 + 米家云服务精简 + DNS 命中率

> Tag: `v2.1.0` · 2026-08-27

## 降载优化（回应"怕占资源"）

- **采集间隔 2 秒 → 3 秒**：面板对路由器的打扰降低约 1/3
- **采集开销可视化**：顶部"采集开销"芯片实时显示单次采集耗时（实测约 70ms）

## 新功能

- **服务精简面板（降负载）**：一键停止米家云服务（messagingagent / mosquitto / xq_info_sync_mqtt），重启路由器自动恢复，零持久化改动；nginx 与系统日志刻意保留
- **DNS 缓存命中率**：直接显示在 DNS 缓存卡片内（彩色分级 + 本地应答/转发/缓存明细），页面加载自动测量，带手动刷新
- **架构**：单文件拆为核心/服务/页面模板三层；默认仅绑定 localhost（LAN 访问需显式开 token）；IP/凭据全部参数化

## 修复

- **DNS 查询记录"坏了"（两处叠加根因）**：
  1. `log-queries` 从未持久化——路由器重启后日志直接断流。已启用（`log-queries` + `log-facility`）并纳入 auto_ssh 恢复清单
  2. 读取窗口被噪音挤占——dnsmasq 每次查询写 query/forwarded/reply 多行，`tail -n 80` 实际只剩 1~2 条查询。改为先 grep 过滤再取窗口，实测列表填满 40 条、3 秒实时刷新
- **截断造出稀疏空洞**：对 dnsmasq 打开中的文件 `>` 截断不重置写偏移（实测攒出 600KB+ NUL 洞）。面板与路由器 cron 的截断均已补 `kill -HUP`
- 命中率统计源随 log-facility 迁移到查询日志文件（原 syslog 路径已无数据）

## 实测记录

- 采集开销约 72ms/次；注入测试查询可实时上屏；查询列表 3.6 秒内 6→12 条自然增长

## v2.0 · 面板大改版：配置真备份 / LED定时 / 状态栏 / 凭据脱敏

> Tag: `v2.0.0` · 2026-08-27

## 面板新功能（panel/router_monitor_ap.py）

- **配置真备份**：一键打包路由器配置（/etc/config、dnsmasq.d、crontab、auto_ssh 自愈脚本、DNS 上游与去广告列表），经 SSH 拉回本机保存；面板内展示历史备份列表（时间/大小），可直接下载
- **LED 面板升级**：
  - 修复开关按钮只能开灯、永远点不灭的 bug（现在真实切换并写回 uci 状态）
  - 新增定时开/关灯设置，时间可在面板编辑并同步到路由器 crontab，页面显示当前计划
- **顶部状态栏**：面板在线/离线实时指示、运行时长、负载、auto_ssh 自愈版本、IPv6 拦截状态
- **负载监控**：负载并入单次 SSH 采集（无额外开销），页面实时更新
- **IPv6 拦截状态**：实测 ip6tables FORWARD REJECT 规则是否生效 + 统计实际使用 IPv6 的设备数
- **单位显示修正**：内存不足 1GB 时显示 MB（原显示 0.1/0.2 GB）；流量图按量级自动切换 KB/s ↔ MB/s
- **界面全面改版**：渐变深色背景、卡片阴影与悬停效果、胶囊状态条、渐变按钮、输入框聚焦高亮、窄屏自适应
- **安全加固**：新增 /download 下载路由（含路径安全检查）；未提供密码时给出明确提示

## 重要修复与注意

- **paramiko 必须 <4**：实测确认 paramiko 5.0 起彻底移除 ssh-rsa（老 dropbear 2017.75 唯一支持的主机密钥算法），升级后面板将无法连接路由器。新增 `requirements.txt` 锁定 `paramiko<4`
- 修复旧版面板使用 `os.environ` 但未导入 `os` 的潜在启动错误

## 凭据脱敏（本次重点）

- 面板脚本、主路由面板、部署脚本、自救手册、使用说明中的硬编码路由器密码全部移除，改为环境变量 `ROUTER_PASSWD` 或占位符；仓库不含任何真实凭据

## 文档

- README 精简，新增 paramiko 版本警告与 v2 版本记录

## v1.0 · AX3000E 中继模式全套（SSH自愈 v4）

> Tag: `v1.0.0` · 2026-08-27

## 小米 AX3000E 中继/AP 模式留存套件

**内含**：SSH 解锁与固化流程、v4 自愈脚本、双版监控面板、一键部署工具、去广告/DNS 配置副本、《自救手册》。

### 附件说明
`auto_ssh.sh` —— 放到路由器 `/data/auto_ssh/` 后执行：

```sh
chmod +x /data/auto_ssh/auto_ssh.sh
/bin/sh /data/auto_ssh/auto_ssh.sh install
```

其余文件按需从本页 Source code (zip) 下载。

### 特性
- 三层自愈：firewall.include + cron 兜底 + 脚本内重试（冷启动实测通过）
- 去广告列表本地缓存 + 超48小时才联网刷新
- 仅适用于官方固件 **1.0.24**；升级固件会失效。详见 README 与《自救手册》。
